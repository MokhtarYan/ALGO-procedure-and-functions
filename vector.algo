PROCEDURE vectors-ortho()
VAR
    n: INTEGER;
    v1 : ARRAY_OF INTEGER[n];
    v2 : ARRAY_OF INTEGER[n];
    sum: INTEGER:=0;
BEGIN
Read(n);
FOR i FROM 0 TO n-1 STEP 1  DO
    sum:= sum+v1[i]*v2[i];
END_FOR
IF (sum=0) THEN
    Write('The vectors are orthogonal')
ELSE
    Write('The vectors are not orthogonal')
END_IF
    
END
